{
  "name": "shepherd",
  "version": "0.1.0",
  "private": true,
  "description": "Medical document summarizer — plain-language patient summaries plus clinician highlights. Synthetic data only.",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.72.0"
  },
  "scripts": {
    "dev": "vercel dev"
  }
}
